/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { HIGHER_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const BECOME_KING_DISCRIMINATOR = new Uint8Array([
  115, 206, 166, 150, 156, 183, 194, 51,
]);

export function getBecomeKingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(BECOME_KING_DISCRIMINATOR);
}

export type BecomeKingInstruction<
  TProgram extends string = typeof HIGHER_PROGRAM_ADDRESS,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountGameState extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPlayer extends string
        ? WritableSignerAccount<TAccountPlayer> &
            AccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountGameState extends string
        ? WritableAccount<TAccountGameState>
        : TAccountGameState,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type BecomeKingInstructionData = {
  discriminator: ReadonlyUint8Array;
  multiplierBps: bigint;
};

export type BecomeKingInstructionDataArgs = { multiplierBps: number | bigint };

export function getBecomeKingInstructionDataEncoder(): FixedSizeEncoder<BecomeKingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["multiplierBps", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: BECOME_KING_DISCRIMINATOR }),
  );
}

export function getBecomeKingInstructionDataDecoder(): FixedSizeDecoder<BecomeKingInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["multiplierBps", getU64Decoder()],
  ]);
}

export function getBecomeKingInstructionDataCodec(): FixedSizeCodec<
  BecomeKingInstructionDataArgs,
  BecomeKingInstructionData
> {
  return combineCodec(
    getBecomeKingInstructionDataEncoder(),
    getBecomeKingInstructionDataDecoder(),
  );
}

export type BecomeKingAsyncInput<
  TAccountPlayer extends string = string,
  TAccountGameState extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  player: TransactionSigner<TAccountPlayer>;
  gameState?: Address<TAccountGameState>;
  /** The vault PDA receiving SOL */
  vault?: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  multiplierBps: BecomeKingInstructionDataArgs["multiplierBps"];
};

export async function getBecomeKingInstructionAsync<
  TAccountPlayer extends string,
  TAccountGameState extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HIGHER_PROGRAM_ADDRESS,
>(
  input: BecomeKingAsyncInput<
    TAccountPlayer,
    TAccountGameState,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  BecomeKingInstruction<
    TProgramAddress,
    TAccountPlayer,
    TAccountGameState,
    TAccountVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? HIGHER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    player: { value: input.player ?? null, isWritable: true },
    gameState: { value: input.gameState ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.gameState.value) {
    accounts.gameState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            103, 97, 109, 101, 95, 115, 116, 97, 116, 101, 95, 118, 50,
          ]),
        ),
      ],
    });
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([118, 97, 117, 108, 116, 95, 118, 50]),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.gameState),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getBecomeKingInstructionDataEncoder().encode(
      args as BecomeKingInstructionDataArgs,
    ),
    programAddress,
  } as BecomeKingInstruction<
    TProgramAddress,
    TAccountPlayer,
    TAccountGameState,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type BecomeKingInput<
  TAccountPlayer extends string = string,
  TAccountGameState extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  player: TransactionSigner<TAccountPlayer>;
  gameState: Address<TAccountGameState>;
  /** The vault PDA receiving SOL */
  vault: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  multiplierBps: BecomeKingInstructionDataArgs["multiplierBps"];
};

export function getBecomeKingInstruction<
  TAccountPlayer extends string,
  TAccountGameState extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HIGHER_PROGRAM_ADDRESS,
>(
  input: BecomeKingInput<
    TAccountPlayer,
    TAccountGameState,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): BecomeKingInstruction<
  TProgramAddress,
  TAccountPlayer,
  TAccountGameState,
  TAccountVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? HIGHER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    player: { value: input.player ?? null, isWritable: true },
    gameState: { value: input.gameState ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.gameState),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getBecomeKingInstructionDataEncoder().encode(
      args as BecomeKingInstructionDataArgs,
    ),
    programAddress,
  } as BecomeKingInstruction<
    TProgramAddress,
    TAccountPlayer,
    TAccountGameState,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type ParsedBecomeKingInstruction<
  TProgram extends string = typeof HIGHER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    player: TAccountMetas[0];
    gameState: TAccountMetas[1];
    /** The vault PDA receiving SOL */
    vault: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: BecomeKingInstructionData;
};

export function parseBecomeKingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedBecomeKingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      player: getNextAccount(),
      gameState: getNextAccount(),
      vault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getBecomeKingInstructionDataDecoder().decode(instruction.data),
  };
}
